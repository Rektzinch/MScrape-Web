# URL backend gosom/google-maps-scraper yang sudah di-host.
MAPS_API_BASE_URL=https://maps-api.example.com

# web   = Docker Web UI API: POST /api/v1/jobs
# queue = API ber-key: POST /api/v1/scrape
MAPS_API_MODE=web

# Wajib hanya untuk mode queue. Jangan memakai prefix NEXT_PUBLIC_.
MAPS_API_KEY=
